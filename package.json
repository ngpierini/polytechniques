{
  "name": "polytechniques-polymer-discovery",
  "private": true,
  "description": "CI-only tooling for the automated polymer discovery pipeline. Not part of the static site itself - nothing here ships to the browser.",
  "type": "commonjs",
  "scripts": {
    "discover": "node scripts/discover-polymers.js",
    "check-data": "node scripts/check-polymer-data.js",
    "check-thermal": "node scripts/check-thermal-library.js",
    "build-index": "node scripts/build-search-index.js",
    "check-index": "node scripts/build-search-index.js --check",
    "check-chain": "node scripts/check-chain-data.js",
    "backfill": "node scripts/backfill-structures.js",
    "check-brackets": "node scripts/check-bracket-geometry.js"
  },
  "dependencies": {
    "openchemlib": "^8.11.0"
  }
}
